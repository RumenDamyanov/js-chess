{
  "name": "chess-vanilla-ts",
  "version": "1.0.0",
  "description": "TypeScript Chess Implementation",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
  "dev": "concurrently \"tsc --watch\" \"npx http-server dist -p 3002 --cors -c-1\"",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit",
  "start": "npx http-server dist -p 3002 --cors -c-1"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "type": "module"
}
